# Python virtualenv for wireviz. Recreate with:
#   python3 -m venv .venv && .venv/bin/pip install wireviz
.venv/

# Rendered harness drawings and BOMs. Regenerate with ./render -- never edit
# these by hand; models/ is the source of truth.
out/

.DS_Store
