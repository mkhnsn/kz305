# WireViz, pinned to the unstable-studios fork.
#
# The fork adds --strict, which turns two silent-data-loss behaviours into
# errors: a component that no connection set references (dropped from the
# drawing AND the BOM), and duplicate YAML keys (last-wins, no diagnostic).
# build.py depends on that API, so this is pinned to an exact commit rather
# than a branch -- a moving ref could change the guarantees underneath us.
wireviz @ git+https://github.com/unstable-studios/WireViz@4ad5fa62c5b29e686b226ca06f99f488b30fcbd1

# Test runner for the guard tests.
pytest>=8
